# Install alongside main deps:  pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0       # coverage reports

# Linting & formatting
ruff>=0.4
